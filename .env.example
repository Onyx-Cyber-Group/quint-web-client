# Quint web client — environment
# Copy to .env and fill with values from the team. Do NOT commit .env.

# The Quint API + Matrix homeserver endpoint (HTTPS).
# Ask your team lead for the correct staging or dev URL.
VITE_API_HOST=https://localhost
